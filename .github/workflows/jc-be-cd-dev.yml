name: CD - Deploy Spring Boot (BE)

on:
  workflow_call:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.1
        with:
          host: ${{ secrets.DEV_SERVER_HOST }}
          username: root
          key: ${{ secrets.DEV_SERVER_SSH_KEY }}
          envs: DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }},DOCKERHUB_TOKEN=${{ secrets.DOCKERHUB_TOKEN }}
          script: |
            set -e
            IMAGE_REPO="${DOCKERHUB_USERNAME}/jobcal-be"
            docker login -u "${DOCKERHUB_USERNAME}" -p "${DOCKERHUB_TOKEN}"
            docker pull "${IMAGE_REPO}:latest"
            docker stop jobcal-be || true
            docker rm jobcal-be || true
            docker run -d --name jobcal-be -p 8080:8080 --restart unless-stopped "${IMAGE_REPO}:latest"

